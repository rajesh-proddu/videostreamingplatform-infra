apiVersion: v2
name: external-secrets-config
description: ClusterSecretStore pointing at AWS SSM Parameter Store. Applied after external-secrets-operator is ready.
type: application
version: 0.1.0
appVersion: "1.0"
