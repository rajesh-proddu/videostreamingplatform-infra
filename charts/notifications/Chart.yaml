apiVersion: v2
name: notifications
description: Realtime notifications platform — Kafka rule engine + web channel (MySQL inbox + Redis) + WebSocket gateway.
type: application
version: 0.1.0
appVersion: "1.0"
